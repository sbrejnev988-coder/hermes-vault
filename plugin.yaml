name: hermes-vault
version: 2.1.0
description: "Encrypted backend for executor plugins. The model can only run a non-sensitive health check."
author: "sbrejnev988-coder"
license: "MIT"

provides_tools:
  - vault_health

provides_modules:
  - executor_bridge
